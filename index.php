<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Corentin Hillion">
    <meta name="author" content="Corentin Hillion">
    <title>Corentin Hillion</title>
    <link rel="icon" href="/favicon.ico">
    <style>
      :root {
        color-scheme: light dark;
        --bg: #0f1115;
        --fg: #f5f5f7;
        --muted: #9aa0aa;
        --accent: #6ea8fe;
      }
      * { box-sizing: border-box; }
      html, body {
        height: 100%;
        margin: 0;
      }
      body {
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--bg);
        color: var(--fg);
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        text-align: center;
        padding: 24px;
      }
      main {
        max-width: 32rem;
      }
      h1 {
        font-size: 1.5rem;
        font-weight: 600;
        margin: 0 0 0.75rem;
      }
      p {
        color: var(--muted);
        line-height: 1.6;
        margin: 0 0 1.5rem;
      }
      a.contact {
        color: var(--accent);
        text-decoration: none;
        font-weight: 500;
        border: 1px solid var(--accent);
        border-radius: 999px;
        padding: 0.5rem 1.25rem;
        display: inline-block;
        transition: background-color 0.15s ease, color 0.15s ease;
      }
      a.contact:hover {
        background-color: var(--accent);
        color: #0f1115;
      }
    </style>
  </head>
  <body>
    <main>
      <h1>Corentin Hillion</h1>
      <p>This website is currently closed for now.<br>Thanks for stopping by &mdash; check back later.</p>
      <a class="contact" href="https://github.com/Cocololco" target="_blank" rel="noopener">Get in touch on GitHub</a>
    </main>
  </body>
</html>
